import { RegisterUserFromEmailPassword } from './register-user-from-email-password';

describe('RegisterUserFromEmailPassword', () => {
  it('should be defined', () => {
    expect(new RegisterUserFromEmailPassword()).toBeDefined();
  });
});
